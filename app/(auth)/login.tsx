import { PlaceholderScreen } from '../../src/components/PlaceholderScreen';

export default function LoginScreen() {
  return (
    <PlaceholderScreen
      title="Ingresar"
      description="Login con teléfono + código SMS (Supabase OTP). Pendiente de credenciales de Supabase."
    />
  );
}
