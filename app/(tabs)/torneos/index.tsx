import { PlaceholderScreen } from '../../../src/components/PlaceholderScreen';

export default function TorneosScreen() {
  return (
    <PlaceholderScreen
      title="Torneos"
      description="Acá va el calendario de torneos: fecha, rival, lugar, resultado."
    />
  );
}
